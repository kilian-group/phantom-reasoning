# phantom-reasoning

**Setup instructions:**
```bash
conda create -n rsn python=3.12
# NOTE: vllm automatically installs pytorch
pip install vllm
pip install flash-attn --no-build-isolation
pip install deepspeed irl wandb
```
